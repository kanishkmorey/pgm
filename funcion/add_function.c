#include <stdio.h>

void add;

void main()
{
	int num1, num2, sum = 0;
	printf("\nEnter two number to add: ");
	scanf("%d %d", &num1, &num2);
	add(unm1, num2, sum);
	printf("\nThe sum is : %d\n\n", sum);
}

void add(int a, int b, int c)
{
	c = a + b;
	printf("\nThe sum is : %d\n\n", c);
}